{
  "name": "docus-starter",
  "scripts": {
    "dev": "nuxt dev --extends docus",
    "build": "nuxt build --extends docus",
    "generate": "NUXT_APP_BASE_URL=/ nuxt build --prerender --extends docus"
  },
  "dependencies": {
    "@nuxt/scripts": "^0.13.2",
    "@unhead/vue": "^2.1.2",
    "better-sqlite3": "^12.5.0",
    "docus": "latest",
    "nuxt": "^4.2.2"
  }
}
