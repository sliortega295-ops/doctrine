{
  "name": "open-doctrine",
  "scripts": {
    "dev": "nuxt dev --extends docus",
    "build": "nuxt build --extends docus",
    "generate": "nuxt build --prerender --extends docus",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@unhead/vue": "^2.1.2",
    "better-sqlite3": "^12.5.0",
    "docus": "latest",
    "nuxt": "^4.2.2"
  },
  "devDependencies": {
    "@nuxt/eslint": "^1.13.0",
    "@nuxt/scripts": "^0.13.2"
  }
}
